{{- if .Values.ec2 }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-env
data:
  content: |
    APP_BASE_PATH="{{ .Values.ec2.appBasePath }}"
    VPC_HOST="{{ .Values.topo.vpc_host }}"
{{- end }}
